#include "main.h"
/**
 * print_sign - defintion for the function
 *
 * @n: int for the number entered
 *
 * Return: 0
 */
int _abs(int)
{
	int n;

	if (int >= 0)
	{
		_putchar(int);
		return (0);
	}
	else if (int > 0)
	{
		n = int * -1; 
		_putchar('+');
		return (1);
	}
	return (1);
}
