#include "main.h"
#include <stdio.h>
/**
 * main - fizzbuzz test program
 *
 * @i: counter
 *
 * Return: always 0
 */
int main(void)
{
	for (i = 1; i <= 100; i++)
	{
		if ((x % 15) == 0)
		{
			printf("FizzBuzz");
		}
		else if ((x % 5) == 0)
		{
			printf("Buzz");
		}
		else if ((x % 3) == 0);
		{
			printf("Fizz");
		}
		else
		{
			putchar(i);
		}
	}
	Return (0);
}
